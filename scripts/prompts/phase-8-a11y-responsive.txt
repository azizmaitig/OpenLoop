Task: Add accessibility annotations and polish responsive behavior in the calendar-app at D:\projects\obsidian\second brain\10-Projects\11-Active\calendar-app.

Load impeccable skill. Run /impeccable harden src/. Run /impeccable adapt src/App.css.

ACCESSIBILITY:
1. Add aria-labels to ALL buttons: nav arrows, today button, view tabs, event chips, close buttons.
2. Add role='dialog' and aria-modal='true' to EventModal.
3. Add role='search' to SearchBar.
4. Ensure all interactive elements are keyboard-focusable (tabindex where needed).
5. Add focus-visible ring styles (2px accent color with 2px offset).
6. Add prefers-reduced-motion media query that disables ALL animations.
7. Add prefers-color-scheme media query as fallback for JS-based dark mode.

RESPONSIVE:
1. Check month view at 480px: days should be compact but readable.
2. Check week/day view at 768px: time labels should not overflow.
3. Modals at <480px: full-width, less padding.
4. Sidebar (AgendaSidebar + MiniCalendar) at <768px: hide behind hamburger.
5. QuickAdd and SearchBar at <480px: full-width.
6. StatsDashboard at <768px: single column charts.

ADDITIONAL POLISH:
1. Loading state for ICS import (can be slow with large files).
2. Error state display if localStorage is full.
3. Tooltips on icon-only buttons (title attribute or aria-label).
4. Print stylesheet improvements (already exists, verify it works).

Use CSS design tokens. No functionality changes. Run npm run build to verify.
