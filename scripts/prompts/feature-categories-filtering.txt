Task: Add event Categories + filtering to the calendar-app at D:\projects\obsidian\second brain\10-Projects\11-Active\calendar-app.

SCOPE (implement ONLY this; do not refactor unrelated code):
1. types.ts: add `export type EventCategory = 'work' | 'personal' | 'health' | 'social' | 'other';`
   and `category?: EventCategory` to `CalendarEvent`. Keep the existing `color` field for
   backward compatibility (default color derives from category when color is absent).
2. Define a CATEGORIES config (e.g. in a new `src/utils/categories.ts` or inside types.ts):
   each entry = { id: EventCategory; label: string; colorToken: string } mapping to OKLCH
   tokens. Add the category color tokens to `src/index.css` (e.g. `--cat-work`,
   `--cat-personal`, ...) using the existing OKLCH convention. Do NOT hardcode hex.
3. New component `src/components/CategoryFilter.tsx`: a list of toggle chips/checkboxes for
   each category. Lifted filter state lives in App.tsx (Set<EventCategory>) and is persisted
   to localStorage under key `calendar-category-filter`. Show it in the sidebar (near
   MiniCalendar/Agenda) or the action bar — your call, keep it tidy.
4. Filtering must apply everywhere events are read: MonthView, WeekView, DayView, YearView,
   AgendaSidebar, SearchBar results, StatsDashboard, and ICS export (visibleEvents in
   App.tsx). Filter by intersecting the active category set; "all off" = show all.
5. EventModal.tsx: add a category <select> bound to the new field; default to 'other'.
6. utils/nlpParser.ts: detect a category from quick-add text — support `#work`/`#personal`
   style tags and a few keywords (e.g. "meeting"/"work" -> work). Keep it conservative.
7. Templates (useEvents saveTemplate / handleUseTemplate): carry the category through.

CONSTRAINTS:
- Use existing CSS design tokens (var(--*)) and the DESIGN.md system. No hardcoded colors.
- Keep changes minimal and within the files listed. No unrelated refactors.
- Ensure keyboard + screen-reader accessibility: toggle buttons need aria-pressed / labels,
  visible focus ring.
- Do not change the data storage shape in a way that breaks existing events (category is optional).

VERIFY: run `npm run build` in the calendar-app dir and ensure it passes with no TypeScript errors.
