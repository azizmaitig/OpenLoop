Task: Add bulk edit / multi-select to the calendar-app at D:\projects\obsidian\second brain\10-Projects\11-Active\calendar-app.

SCOPE (implement ONLY this; do not refactor unrelated code):
1. Add a "Select" mode toggle to the action bar in App.tsx (aria-pressed, labelled). When
   active, event chips across MonthView, WeekView, DayView, AgendaSidebar, and YearView
   render a checkbox / selection affordance instead of (or in addition to) opening the modal.
2. Selection state in App.tsx: `Set<string>` of event ids. Clicking a chip toggles selection.
   Provide a bulk-action toolbar (appears when >=1 selected): Delete, Duplicate, Move
   (+/- N days), and Set Category / Set Color. Wire these to useEvents (addEvent / updateEvent
   / deleteEvent).
3. Bulk delete must be a SINGLE undoable action via the existing useUndoRedo hook (one
   history entry covering all deleted events), not N separate entries.
4. Keyboard: Esc clears selection; Ctrl/Cmd+A selects all currently visible events.
5. Clicking a selected chip's body (not the checkbox) in select mode should NOT open
   EventModal. Outside select mode, existing click-to-edit behavior is unchanged.
6. Accessibility: selection checkboxes need accessible labels; the toolbar buttons need
   aria-labels; announce selection count via aria-live.

CONSTRAINTS:
- Use existing CSS design tokens (var(--*)) and the DESIGN.md system. No hardcoded colors.
- Touch the fewest files needed (App.tsx + the view components + useEvents/useUndoRedo).
  No unrelated refactors.
- Keep selection ephemeral (not persisted). Select mode auto-exits after a bulk delete.
- Preserve existing drag-and-drop and single-event interactions.

VERIFY: run `npm run build` in the calendar-app dir and ensure it passes with no TypeScript errors.
