Task: Add purposeful animations and micro-interactions to the calendar-app at D:\projects\obsidian\second brain\10-Projects\11-Active\calendar-app.

Load impeccable skill. Run /impeccable animate src/

ANIMATIONS TO ADD:
1. View transitions: Smooth opacity+transform (~300ms ease-out-quart) when switching between Month/Week/Day/Year views in App.tsx.

2. Modal enter/exit: Fade in overlay + scale up modal on open. Reverse on close. Use CSS transitions, not animation libraries.

3. Event chip hover: Subtle lift (translateY(-1px)) + shadow increase on hover. Transition: 200ms ease-out.

4. QuickAdd success: Brief green border flash or checkmark icon that fades.

5. Search results: Sequential fade-in of dropdown items (max 3 items, 50ms stagger).

6. Today button pulse: Subtle ring animation on Today button click.

7. Dark mode toggle: Smooth background/text color transitions on body (transition: background-color 300ms, color 300ms).

RULES:
  - Use ease-out-quart or ease-out-expo curves. No bounce, no elastic.
  - Every animation MUST have @media (prefers-reduced-motion: reduce) alternative.
  - Do NOT animate layout properties (width, height, top, left) — only transform and opacity.
  - No external animation libraries — use CSS transitions and keyframes only.
  - Stagger identical items only when meaningful (max 50ms delay between items).

Run npm run build to verify.
