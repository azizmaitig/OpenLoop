Task: Final-polish pass — interaction states & accessibility across calendar-app at D:\projects\obsidian\second brain\10-Projects\11-Active\calendar-app.

Load the impeccable skill. This is phase 9b of the calendar design loop (finish-only). Prior design-system, dark-mode, and a11y/responsive phases are done; this pass hardens interaction states.

SCOPE:
1. Verify every button / link / interactive element has hover + focus-visible + active states.
2. Verify EventModal (and any modal) closes on Escape key press.
3. Verify keyboard navigation: Tab cycles through all interactive elements in logical order; no focus traps outside modals.
4. Verify no text overflows its container at any breakpoint (320px, 768px, 1280px).
5. Verify `prefers-reduced-motion` disables all animations/transitions.

CHECKLIST:
- [ ] All interactive elements have :hover, :focus-visible, :active
- [ ] Modals close on Escape
- [ ] Tab order is logical and complete
- [ ] No text overflow at 320/768/1280px
- [ ] Reduced-motion disables animation

Edit only CSS / component event handlers needed for the above. Run `npm run build` to verify. Output a summary of fixes.