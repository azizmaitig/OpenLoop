Task: Add a first-run onboarding tour + persistent Help to the calendar-app at D:\projects\obsidian\second brain\10-Projects\11-Active\calendar-app.

SCOPE (implement ONLY this; do not refactor unrelated code):
1. First-run detection: a localStorage flag `calendar-onboarded`. If absent, show the tour
   automatically on first load. Set the flag when the tour is completed or skipped.
2. New component `src/components/OnboardingTour.tsx`: a step-by-step overlay highlighting
   these parts of the UI (in order):
   - QuickAdd bar (natural-language event creation)
   - View tabs (month / week / day / year)
   - Keyboard hints (n = next, p = prev, t = today, c = create)
   - Theme toggle (dark mode)
   - Import / Export ICS actions
   Each step: short text + Next / Back / Skip. Use a spotlight or simple highlighted card —
   keep it lightweight, no heavy animation.
3. Persistent "Help" button (?) in the header (CalendarHeader.tsx) that re-opens the tour at
   any time.
4. Accessibility: the tour is a modal dialog (role="dialog", aria-modal="true", labelled
   title), traps focus, closes on Esc, announces the current step via aria-live. Buttons have
   aria-labels.
5. Respect `prefers-reduced-motion`: disable any transitions/animations in the tour.
6. Non-intrusive: never block the underlying calendar; tour sits above with a dismissible
   backdrop. Do not alter existing layout or other components' behavior.

CONSTRAINTS:
- Use existing CSS design tokens (var(--*)) and the DESIGN.md system. No hardcoded colors.
- New component only + small wiring in App.tsx / CalendarHeader.tsx. No unrelated refactors.
- Keep the tour copy concise and accurate to the actual shortcuts/features.

VERIFY: run `npm run build` in the calendar-app dir and ensure it passes with no TypeScript errors.
